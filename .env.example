# Intersight API Configuration
# Required: Your Intersight API Key ID
API_KEY=your_api_key_here

# Required: Your Intersight API Secret Key (path to private key file or key content)
API_SECRET=your_api_secret_here

# Optional: Intersight API Endpoint (default: https://intersight.com)
IS_ENDPOINT=https://intersight.com

# Optional: Safe Mode - prevents destructive operations when true (default: false)
# Set to 'true' to prevent deletions during import operations
SAFE_MODE=false

# Optional: Log Level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_LEVEL=INFO

# Optional: Output directory for export operations (default: ./output)
FILES_DIR=./files
